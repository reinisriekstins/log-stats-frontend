'use strict'

mocha.setup({timeout: 3000})

describe('Test of function dryGet in index.js', function () {
  /*
  before(() => {
    console.log('before')
  })

  beforeEach(() => {
    console.log('beforeEach')
  })
  */
  it('is an asynchronous test', function (done) { 
    //dryGet('/json/12345').then(res => {
    //  console.log(res)
    //  done()
    //})
    console.log($)
    done()
  })
  /*
  afterEach(() => {
    console.log('afterEach')
  })

  after(() => {
    console.log('after')
  })
  */
})